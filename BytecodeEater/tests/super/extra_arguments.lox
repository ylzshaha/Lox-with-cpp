// Derived.foo()
//[line 12] Error at ')': Expected 2 args but got 4.
// 70
class Base {
  foo(a, b) {
    print "Base.foo(" + a + ", " + b + ")";
  }
}
class Derived < Base {
  foo() {
    print "Derived.foo()";
    super.foo("a", "b", "c", "d");
  }
}
Derived().foo();
